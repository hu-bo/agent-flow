import { WorkspacePlaceholder } from './WorkspacePlaceholder';

export function AgentPage() {
  return <WorkspacePlaceholder label="AGENT_WORKSPACE" description="Agent team controls are being prepared." />;
}
