# API Gateway

PORT=9010
DATABASE_URL=postgres://llm_api_gateway_user:pass@localhost:15000/llm_api_gateway?sslmode=disable

# 64 hex chars (32 bytes) for AES-256-GCM encryption of provider API keys
# Generate with: openssl rand -hex 32
ENCRYPTION_KEY=xxx
