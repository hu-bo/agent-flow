
specs流程

user: 首次query
agent: Including Steering Documents AGENTS.md
这是一个关于消息渲染过滤的功能需求。让我先确认一下：

根据你的描述，这是一个新功能需求（"如何做到"意味着需要实现一个不存在的功能）。

需求理解： 在 LLM chat 场景中，当 mode = Spec 时：

问题：LLM 返回的设计 markdown 内容会同时出现在前端 chat 面板中
期望：设计内容只出现在左侧的 spec markdown 文件中，不出现在 chat 面板

弹窗提示：
构建一个新功能
修复一个bug
自定义

弹窗提示：
开始design
自定义

弹窗提示：
开始实现task
自定义

关键设计决策
流式更新：左侧 Markdown 编辑器实时显示 LLM 输出进度
分离持久化：Spec 文档独立存储，不混入聊天历史
Phase 关联：spec_doc_update 事件携带 doc_type，前端根据当前 phase 路由到正确面板
摘要消息：完成后生成一条简短消息到 chat，保持对话连贯性

方案：Spec 模式下 Markdown 内容分流机制
核心思路
在 Spec 模式下，LLM 返回的设计文档内容（requirements.md、design.md、tasks.md）需要：

不显示在 Chat 面板：用户在对话中只看到简短确认消息
实时更新到左侧 Spec Doc Pane：Markdown 编辑器中显示完整设计内容


┌─────────────────────────────────────────────────────────────┐
│                     LLM Response Stream                       │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                   web-server (Fastify)                        │
│  ┌─────────────────────────────────────────────────────┐    │
│  │  spec-service.ts                                      │    │
│  │  - 解析 LLM 输出                                       │    │
│  │  - 识别 spec-doc 内容块                                │    │
│  │  - 生成两种输出:                                       │    │
│  │    ① spec_doc_update 事件 → 左侧面板                   │    │
│  │    ② text 消息 → chat 面板 (仅 summary)               │    │
│  └─────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
                              │
                    SSE Stream
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                   web-ui (React)                              │
│  ┌────────────────┐         ┌────────────────────────────┐  │
│  │  ChatPanel     │         │  SpecDocPane (MDEditor)     │  │
│  │                │         │                              │  │
│  │  显示:         │         │  显示:                       │  │
│  │  - 用户消息    │         │  - 完整 requirements.md     │  │
│  │  - summary    │         │  - 完整 design.md           │  │
│  │  - 确认提示   │         │  - 完整 tasks.md            │  │
│  │                │         │  (实时流式更新)              │  │
│  └────────────────┘         └────────────────────────────┘  │
└─────────────────────────────────────────────