{
  "name": "@agent-flow/context-compressor",
  "version": "0.1.0",
  "private": true,
  "description": "Context compression engine: auto-compact, micro-compact, summary generation",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@agent-flow/model-contracts": "workspace:*",
    "@agent-flow/model-gateway": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
